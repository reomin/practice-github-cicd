name: Manual
on: workflow_dispatch
inputs:
  greeting:
    type: string
    description: "Greeting message"
    default: "Hello"
    require: true #入力パラメーターの必須化
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{inputs.greeting}} world" #入力パラメーターの使用
